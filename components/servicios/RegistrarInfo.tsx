import { APIServicio } from '@/lib/servicios/api_servicios'
import React from 'react'
import { Text, View } from 'react-native'

const RegistrarInfo=(item: APIServicio) => {
    return (
        <View>
            <Text>RegistrarInfo</Text>
        </View>
    )
}

export default RegistrarInfo