import { Servicio } from '@/lib/api'
import React from 'react'
import { Text, View } from 'react-native'

const Listado=(servicios: Servicio[]) => {
    return (
        <View>
            <Text>Listado</Text>
        </View>
    )
}

export default Listado